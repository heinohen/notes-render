{
  "name": "notes-render",
  "version": "1.0.0",
  "description": "hello world",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "buid:ui": "rm -rf dist && cd ./front && npm run build && cp -r dist ../",
    "deploy:full": "npm run build:ui && git add . %% git commit -m uibuild && git push"
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henq",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
